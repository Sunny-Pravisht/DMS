fastapi==0.136.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.13.1
chromadb==0.4.22
# Pulled in by chromadb, but listed explicitly because embedding_service.py
# uses the ONNX MiniLM model directly for local (Groq-compatible) embeddings.
onnxruntime>=1.16.0
tokenizers>=0.15.0
# Groq is reached through the OpenAI-compatible SDK (custom base_url).
# 1.3.7 predates the `base_url` kwarg and vision content blocks.
openai==1.59.6
pytesseract==0.3.10
Pillow==12.2.0
python-multipart==0.0.28
watchdog==4.0.0
pydantic==2.12.5
pydantic-settings==2.12.0
bcrypt==4.1.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.5.0
aiofiles==23.2.1
jinja2==3.1.6
pdf2image==1.17.0
PyPDF2==3.0.1
python-magic==0.4.27
# The Document Studio renders composed documents to PDF. ReportLab is pure
# Python with prebuilt wheels, so no system libraries are needed on any host.
reportlab==4.2.5
# Lets bleach filter inline CSS instead of discarding the style attribute,
# which is how alignment and colour survive from the editor into the PDF.
tinycss2==1.5.1
# Renders a PDF page to an image so the signature placement editor can show
# the real page behind the blocks. PDFium is BSD/Apache licensed and ships as
# a self-contained wheel - deliberately not PyMuPDF, which is AGPL.
pypdfium2==5.12.1
numpy==1.26.4
loguru==0.7.2
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.26.0
tabulate==0.9.0
bleach==6.1.0
psutil==5.9.8
schedule==1.2.1
